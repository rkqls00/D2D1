#include "_StdAfx.h"
#include "Collide.h"
Collide::~Collide() {
	
}
/*BOOL IsPointInRect(RECT rt, POINT pt)
{
	if ((rt.left <= pt.x && rt.right >= pt.x) &&
		(rt.top <= pt.y && rt.bottom >= pt.y))
	{ 
		return TRUE;
	}
	return FALSE;
}*/
